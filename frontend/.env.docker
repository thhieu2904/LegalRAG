# Docker Production Environment Variables
# Used when building Docker image

# ============= API CONFIGURATION =============
# Direct service URLs (no Kong Gateway in Docker setup)
VITE_API_BASE_URL=http://localhost:8001

# ============= APP CONFIGURATION =============
VITE_APP_TITLE=LegalRAG System

# ============= FILE UPLOAD =============
# Maximum file size in bytes (10MB)
VITE_MAX_FILE_SIZE=10485760
